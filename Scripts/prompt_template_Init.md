任务：在指定的设计空间内生成 <<NUM_POINTS>> 个初始设计点。

变量定义与范围为：
<<RANGES>>

要求：

生成的点应在空间中尽可能均匀分布，避免聚集。

必须生成 <<NUM_POINTS>> 个不同的点。

输出仅包含一个 JSON 列表。不要包含任何 markdown 标记（如 ```json）。

输出格式示例：
[
{"x1": 10, "x2": 50, ...},
{"x1": 80, "x2": 20, ...},
...
]

请输出：